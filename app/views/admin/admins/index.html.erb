<h3>Active Admins:</h3>

<ul>
	<% @admins.each do |a| %>
		<li><%= link_to "#{a.username}", admin_admin_path(a)  %> </li>
	<% end %>
</ul>

<%= link_to "Register a new admin",  new_admin_admin_path, :class => "btn btn-default" %>
