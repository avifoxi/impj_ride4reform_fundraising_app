<h3>Your Donation to <%= @rider.full_name %></h3>

<%= render 'shared/form_errors' %>

<%= simple_form_for @donation, :url => persistent_rider_profile_donations_path(@rider), :method => :post do |f| %>
  
  <%= render 'form', :f => f %>
  
  <%= f.button :submit %>
<% end %>