<h3>Edit Donation</h3>

<%= simple_form_for @donation do |f| %>
	
	<%= render 'form', f: f %>
	
	<%= f.button :submit %>

<% end %>

<hr>
<%= button_to "Delete Donation without Payment?", @donation, :method => :delete, class: 'btn btn-danger' %>