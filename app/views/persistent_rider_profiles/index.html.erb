<h3>Our Riders for <%= @year.year %></h3>

<div class="row">
  	<% @riders.each do |rider| %>
			<%= render 'thumbnail', :rider => rider  %>
		<% end %>
</div>

<script>
  $(document).ready(function(){
    var highest = 0,
    	$asyms = $('.prp-image');
    
    $asyms.each(function(i, el){ 
      var h = $(el).height(); 
      h > highest ? highest = h : null;
    });
    $asyms.each(function(i, el){
    	$(el).height( highest );
    });
  });


</script>