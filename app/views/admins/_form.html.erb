<%= simple_form_for @admin do |f| %>
  <%= f.input :username, label: 'Choose Admin user name' %>
  <%= f.input :password, hint: 'No special characters. At leats 8 characters long.' %>
  <%= f.input :email, placeholder: 'admin@domain.com' %>
  <%= f.button :submit %>
<% end %>

